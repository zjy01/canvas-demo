/**
 * Created by zjy on 2016/4/22.
 */
window.onload = function () {
    //初始化获取canvas和context
    this.canvas = document.getElementById('canvas');
    paper.init(canvas);
    write.init(canvas);

};